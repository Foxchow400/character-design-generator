var gender;
var species;

function $(div) {
    return document.getElementById(div);
}

function display(g) {
    $("output").innerHTML = "<section>" +
    "Eat a bag of dicks." +
    "</section>"
}

function generate() {
    gender = $("gender").value;
    function display(gender);
}